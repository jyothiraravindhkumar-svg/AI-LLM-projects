# Streamlit Q&A Chatbot

A minimal web app: type a question, get an LLM-generated answer, via Streamlit + LangChain's OpenAI wrapper.

## Run
```bash
pip install -r requirements.txt
export OPENAI_API_KEY="your-key-here"
streamlit run app.py
```
